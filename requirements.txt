# Ansible requirements for BA_CyberRange_PoC
# Install with: pip install -r requirements.txt

# Core Ansible (if not installed via package manager)
# ansible>=2.10.0

# Optional: Additional tools for enhanced functionality
# mitmproxy>=8.0.0
# sqlmap>=1.6.0

# Note: Most dependencies are handled by the Ansible playbooks
# This file is for optional Python tools that might be useful 